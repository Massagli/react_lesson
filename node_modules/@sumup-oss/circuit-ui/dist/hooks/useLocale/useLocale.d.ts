import { type Locale } from '../../util/i18n.js';
/**
 * Returns the current browser/system language, and updates when it changes.
 */
export declare function useLocale(customLocale?: Locale): Locale;
