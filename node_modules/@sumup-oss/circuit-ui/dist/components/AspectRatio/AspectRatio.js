"use strict";
import { jsx } from "react/jsx-runtime";
import { forwardRef, Children, cloneElement } from "react";
import { clsx } from "../../styles/clsx.js";
import classes from "./AspectRatio.module.css.js";
const AspectRatio = forwardRef(
  ({ aspectRatio, className, style = {}, children, ...props }, ref) => {
    if (!children) {
      return null;
    }
    const child = Children.only(children);
    if (!aspectRatio) {
      return /* @__PURE__ */ jsx("div", { ref, className, ...props, children: child });
    }
    return /* @__PURE__ */ jsx(
      "div",
      {
        ref,
        className: clsx(classes.base, className),
        style: {
          ...style,
          "--aspect-ratio": `${Math.round(1 / aspectRatio * 100)}%`
        },
        ...props,
        children: cloneElement(child, { className: classes.child })
      }
    );
  }
);
AspectRatio.displayName = "AspectRatio";
export {
  AspectRatio
};
