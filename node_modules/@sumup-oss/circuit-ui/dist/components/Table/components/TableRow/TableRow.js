"use strict";
"use client";
import { jsx } from "react/jsx-runtime";
import { useCallback } from "react";
import { clsx } from "../../../../styles/clsx.js";
import { isEnter, isSpacebar } from "../../../../util/key-codes.js";
import classes from "./TableRow.module.css.js";
function TableRow({ onClick, className, ...props }) {
  const onRowKeydown = useCallback(
    (event) => {
      if (isEnter(event) || isSpacebar(event)) {
        onClick == null ? void 0 : onClick(event);
      }
    },
    [onClick]
  );
  return /* @__PURE__ */ jsx(
    "tr",
    {
      onClick,
      onKeyDown: onClick ? onRowKeydown : void 0,
      tabIndex: onClick ? 0 : void 0,
      className: clsx(classes.base, className),
      ...props
    }
  );
}
export {
  TableRow
};
