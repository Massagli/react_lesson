"use strict";
import { Temporal } from "temporal-polyfill";
import { formatDateTimeToParts, formatDate } from "@sumup-oss/intl";
const TEST_VALUE = new Temporal.PlainDate(2024, 3, 8);
function getDateSegments(locale) {
  const parts = formatDateTimeToParts(TEST_VALUE, locale);
  return parts.map(
    ({ type, value }) => type === "literal" ? { type, value } : { type }
  );
}
function getCalendarButtonLabel(label, date, locale) {
  if (!date) {
    return label;
  }
  return [label, formatDate(date, locale, "long")].join(", ");
}
export {
  getCalendarButtonLabel,
  getDateSegments
};
