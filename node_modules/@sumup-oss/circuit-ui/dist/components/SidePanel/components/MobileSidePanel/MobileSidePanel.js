"use strict";
import { jsx } from "react/jsx-runtime";
import ReactModal from "react-modal";
import { TRANSITION_DURATION } from "../../constants.js";
import classes from "./MobileSidePanel.module.css.js";
import { StackContext } from "../../../StackContext/StackContext.js";
function MobileSidePanel({
  children,
  isBottomPanelClosing,
  isStacked,
  ...props
}) {
  const reactModalProps = {
    className: {
      base: classes.base,
      afterOpen: classes.open,
      beforeClose: classes.closed
    },
    overlayClassName: {
      base: classes.overlay,
      afterOpen: classes["overlay-open"],
      beforeClose: classes["overlay-closed"]
    },
    closeTimeoutMS: TRANSITION_DURATION,
    ...props
  };
  return /* @__PURE__ */ jsx(StackContext.Provider, { value: "var(--cui-z-index-modal)", children: /* @__PURE__ */ jsx(ReactModal, { ...reactModalProps, children }) });
}
export {
  MobileSidePanel
};
