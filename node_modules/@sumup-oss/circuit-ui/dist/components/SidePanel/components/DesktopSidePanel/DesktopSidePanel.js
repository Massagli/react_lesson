"use strict";
import { jsx } from "react/jsx-runtime";
import ReactModal from "react-modal";
import { clsx } from "../../../../styles/clsx.js";
import classes from "./DesktopSidePanel.module.css.js";
import { StackContext } from "../../../StackContext/StackContext.js";
function DesktopSidePanel({
  children,
  isInstantOpen,
  ...props
}) {
  const reactModalProps = {
    ariaHideApp: false,
    className: {
      base: classes.base,
      afterOpen: clsx(classes.open, isInstantOpen && classes.instant),
      beforeClose: classes.closed
    },
    overlayClassName: classes.overlay,
    shouldCloseOnOverlayClick: false,
    closeTimeoutMS: 300,
    ...props
  };
  return /* @__PURE__ */ jsx(StackContext.Provider, { value: "var(--cui-z-index-navigation)", children: /* @__PURE__ */ jsx(ReactModal, { ...reactModalProps, children }) });
}
export {
  DesktopSidePanel
};
