"use strict";
import { formatNumber } from "@sumup-oss/intl";
function formatPlaceholder(placeholder, locale, options) {
  return typeof placeholder === "number" ? formatNumber(placeholder, locale, options) : placeholder;
}
export {
  formatPlaceholder
};
