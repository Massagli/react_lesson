"use strict";
import { jsx } from "react/jsx-runtime";
import { forwardRef } from "react";
import { clsx } from "../../styles/clsx.js";
import classes from "./Numeral.module.css.js";
const Numeral = forwardRef(
  ({ className, as: Element = "p", size = "m", color = "normal", ...props }, ref) => /* @__PURE__ */ jsx(
    Element,
    {
      ...props,
      ref,
      className: clsx(classes.base, classes[size], classes[color], className)
    }
  )
);
Numeral.displayName = "Numeral";
export {
  Numeral
};
