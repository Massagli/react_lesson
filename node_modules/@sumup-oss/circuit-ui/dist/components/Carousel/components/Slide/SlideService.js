"use strict";
import { SLIDE_DIRECTIONS } from "../../constants.js";
function getStackOrder(slideIndex, step, prevStep, slideDirection) {
  const isActive = step === slideIndex;
  const wasActive = prevStep === slideIndex;
  if (slideDirection === SLIDE_DIRECTIONS.FORWARD && isActive || slideDirection === SLIDE_DIRECTIONS.BACK && wasActive) {
    return 2;
  }
  if (slideDirection === SLIDE_DIRECTIONS.FORWARD && wasActive || slideDirection === SLIDE_DIRECTIONS.BACK && isActive) {
    return 1;
  }
  return slideIndex ? -slideIndex : 0;
}
function shouldAnimate(slideIndex, step, prevStep, slideDirection) {
  const isActive = step === slideIndex;
  const wasActive = prevStep === slideIndex;
  if (slideDirection === SLIDE_DIRECTIONS.FORWARD) {
    return isActive;
  }
  if (slideDirection === SLIDE_DIRECTIONS.BACK) {
    return wasActive;
  }
  return false;
}
function getDynamicWidth(width) {
  return width ? `${width}px` : "100%";
}
export {
  getDynamicWidth,
  getStackOrder,
  shouldAnimate
};
