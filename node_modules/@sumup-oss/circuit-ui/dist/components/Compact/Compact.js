"use strict";
import { jsx } from "react/jsx-runtime";
import { forwardRef } from "react";
import { clsx } from "../../styles/clsx.js";
import classes from "./Compact.module.css.js";
const Compact = forwardRef(
  ({
    className,
    as: Element = "p",
    size = "m",
    weight = "regular",
    color = "normal",
    ...props
  }, ref) => /* @__PURE__ */ jsx(
    Element,
    {
      ...props,
      ref,
      className: clsx(
        classes.base,
        classes[size],
        classes[weight],
        classes[color],
        className
      )
    }
  )
);
Compact.displayName = "Compact";
export {
  Compact
};
