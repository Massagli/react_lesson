"use strict";
"use client";
import { jsx, jsxs } from "react/jsx-runtime";
import { forwardRef } from "react";
import { useComponents } from "../ComponentsContext/useComponents.js";
import { utilClasses } from "../../styles/utility.js";
import { clsx } from "../../styles/clsx.js";
import classes from "./CarouselPagination.module.css.js";
const CarouselPagination = forwardRef(({ slides, currentId, type, className, ...props }, ref) => {
  const components = useComponents();
  const Link = components.Link;
  return /* @__PURE__ */ jsx("ul", { ref, className: clsx(classes.base, className), ...props, children: slides.map(({ id, label, ...item }) => {
    let Element;
    if (item.href) {
      Element = Link;
    } else if (item.onClick) {
      Element = "button";
    } else {
      Element = "span";
    }
    return /* @__PURE__ */ jsx("li", { children: /* @__PURE__ */ jsxs(
      Element,
      {
        ...item,
        "aria-current": currentId === id ? type || true : void 0,
        className: clsx(classes.cue, utilClasses.focusVisibleInset),
        children: [
          /* @__PURE__ */ jsx("span", { className: classes.shape }),
          /* @__PURE__ */ jsx("span", { className: utilClasses.hideVisually, children: label })
        ]
      }
    ) }, id);
  }) });
});
export {
  CarouselPagination
};
