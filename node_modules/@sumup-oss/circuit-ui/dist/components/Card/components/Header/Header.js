"use strict";
"use client";
import { jsxs, jsx } from "react/jsx-runtime";
import { forwardRef } from "react";
import { isArray } from "../../../../util/type-check.js";
import { clsx } from "../../../../styles/clsx.js";
import classes from "./Header.module.css.js";
import { CloseButton } from "../../../CloseButton/CloseButton.js";
const CardHeader = forwardRef(
  ({ onClose, children, closeButtonLabel, className, ...props }, ref) => {
    const noHeadline = isArray(children) && !children[0];
    return /* @__PURE__ */ jsxs(
      "header",
      {
        className: clsx(
          classes.base,
          noHeadline && classes["no-headline"],
          className
        ),
        ref,
        ...props,
        children: [
          children,
          onClose && closeButtonLabel && /* @__PURE__ */ jsx(CloseButton, { className: classes.close, onClick: onClose, children: closeButtonLabel })
        ]
      }
    );
  }
);
export {
  CardHeader
};
