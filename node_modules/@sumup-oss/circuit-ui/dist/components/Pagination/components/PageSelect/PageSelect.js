"use strict";
"use client";
import { jsxs, jsx } from "react/jsx-runtime";
import { useId, useCallback, Fragment } from "react";
import classes from "./PageSelect.module.css.js";
import { Select } from "../../../Select/Select.js";
const PageSelect = ({
  label,
  onChange,
  pages,
  currentPage,
  totalPages,
  totalLabel,
  ...props
}) => {
  const descriptionId = useId();
  const pageOptions = pages.map((value) => ({ value, label: `${value}` }));
  const handleChange = useCallback(
    (event) => {
      onChange(Number.parseInt(event.target.value, 10));
    },
    [onChange]
  );
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(
      Select,
      {
        ...props,
        label,
        value: currentPage,
        options: pageOptions,
        onChange: handleChange,
        hideLabel: true,
        "aria-describedby": descriptionId
      }
    ),
    totalLabel && /* @__PURE__ */ jsx("span", { id: descriptionId, className: classes.total, children: totalLabel(totalPages) })
  ] });
};
export {
  PageSelect
};
