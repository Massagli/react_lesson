"use strict";
function getFirstNRows(arr, n) {
  let credit = n;
  const result = [];
  arr.forEach((section) => {
    if (credit > 0) {
      credit -= 1;
      const newFeatures = [
        ...credit < section.features.length ? section.features.slice(0, credit) : section.features
      ];
      credit -= newFeatures.length;
      result.push({ ...section, features: newFeatures });
    }
  }, []);
  return result;
}
function generateFromIndex(data, positions) {
  return positions.map((i) => data[i]);
}
export {
  generateFromIndex,
  getFirstNRows
};
