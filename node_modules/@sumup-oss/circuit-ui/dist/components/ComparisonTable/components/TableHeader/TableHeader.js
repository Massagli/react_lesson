"use strict";
"use client";
import { jsx, jsxs } from "react/jsx-runtime";
import { TierIndicator } from "../../../TierIndicator/TierIndicator.js";
import { clsx } from "../../../../styles/clsx.js";
import classes from "./TableHeader.module.css.js";
import { Compact } from "../../../Compact/Compact.js";
import { Body } from "../../../Body/Body.js";
import { Button } from "../../../Button/Button.js";
const TableHeader = ({
  title,
  description,
  tier,
  className,
  action,
  ...props
}) => /* @__PURE__ */ jsx("th", { scope: "col", className: clsx(classes.wrapper, className), ...props, children: /* @__PURE__ */ jsxs("div", { className: classes.base, children: [
  /* @__PURE__ */ jsxs("div", { className: classes.title, children: [
    /* @__PURE__ */ jsx(Compact, { size: "l", weight: "bold", className: clsx(tier && classes.hide), children: title }),
    tier && /* @__PURE__ */ jsx(TierIndicator, { ...tier, size: "m" })
  ] }),
  description && /* @__PURE__ */ jsx(Body, { size: "s", className: classes.description, children: description }),
  action && /* @__PURE__ */ jsx(
    Button,
    {
      ...action,
      className: classes.action,
      variant: "secondary",
      size: "s"
    }
  )
] }) });
export {
  TableHeader
};
